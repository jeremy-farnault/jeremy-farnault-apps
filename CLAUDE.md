# Project Guidelines

## Verification

**Do not start the dev server or run the app to verify changes.** Run the type-check instead (`bun tsc --noEmit` or `pnpm type-check` from the relevant package). The user will test the UI manually.
