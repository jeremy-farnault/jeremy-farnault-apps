// Schema barrel — re-export all table definitions from here.
// Example: export * from "./users.js";
// Run `pnpm --filter @jf/db db:push` to sync schema to Neon.
// Run `npx better-auth generate --output src/schema/auth.ts` to generate auth tables.
